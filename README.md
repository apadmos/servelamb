# servelamb
A minimalist web server lib that targets deploying entire websites to single Lambda expressions. 
It runs a basic server locally for development that matches the interface of an AWS Lambda expression
